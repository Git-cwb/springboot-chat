#服务器配置
server:
  port: 80
  session-timeout: 60
  tomcat.max-threads: 0
  tomcat.uri-encoding: UTF-8

#spring相关配置--简化页面前后缀
spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    content-type: text/html
    cache: false